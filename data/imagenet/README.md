# ImageNet Preprocessed Data

The preprocessed ImageNet numpy files are not included due to size.
They contain per-image correctness labels and synthetic annotator
scores for 5 ResNet models on the ImageNet validation set.